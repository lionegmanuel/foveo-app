# UPDATES — Registro de sesiones de trabajo (screenzoom)

Changelog contextual de este repo. Cada entrada resume qué se hizo, qué se verificó y qué quedó pendiente. Entradas más recientes arriba.

Retención activa (según `CLAUDE.md`): se conservan solo las últimas 3 secciones de día.

## 2026-08-28 — Fase 3 (Rust escrito, sin compilar todavia — ver pendiente) + setup completo de toolchain

### Sesión 1 — Setup de entorno (Rust/MSVC/pnpm faltaban) + implementación de los 5 items de Fase 3

- **Pedido**: "cómo vamos, tiene que quedar 100% lista" → tras explicar el estado real (Fase 1+2 completas pero sin validación visual de Manuel; Fase 3 sin empezar), Manuel pidió avanzar Fase 3 con criterio de "producción real, completa y útil para un creador de contenido, sin irse al extremo".
- **Bloqueo de entorno descubierto y resuelto**: esta sesión corrió en una máquina/entorno sin `cargo`/`rustc` ni `pnpm` en PATH (sí había `node`). Se instaló `rustup` (`winget install Rustlang.Rustup`) y Visual Studio Build Tools 2022 con el workload C++ (`winget install Microsoft.VisualStudio.2022.BuildTools`, workload `VCTools` + Windows 11 SDK) — el instalador de Build Tools **pidió reiniciar Windows para terminar de registrarse**, y Manuel pidió explícitamente no reiniciar hasta que esta sesión termine todo el trabajo. `pnpm` se activó vía `corepack enable --install-directory` (el install-directory por default, `D:\Program Files\nodejs`, no es escribible sin admin en este entorno).
- **Consecuencia real, no cosmética**: `cargo build`/`test`/`clippy` no corrieron ni una sola vez en esta sesión (el linker de MSVC no queda operativo hasta el reinicio pendiente) — **todo el código Rust de abajo está escrito y revisado a mano con mucho cuidado (incluso descargando y leyendo el source real de `windows-capture` 2.0.1 para verificar firmas exactas, mismo hábito que con `wgpu` en sesiones previas), pero no verificado por el compilador**. Sí se verificó `cargo fmt --check` (no necesita el linker, solo parsea — confirma que no hay errores de sintaxis en ningún archivo tocado) y el lado TypeScript completo sí se verificó de verdad (`pnpm exec tsc -b --noEmit`, `pnpm exec vite build`, `pnpm lint`, los tres limpios). **Primer paso obligatorio de la próxima sesión, apenas Manuel reinicie**: `cargo test --workspace` + `cargo clippy --workspace --all-targets -- -D warnings` y arreglar lo que aparezca antes de tocar nada más.
- **Selección de ventana como fuente** (`crates/capture`): `CaptureSource` (`PrimaryMonitor`/`Monitor(idx)`/`Window(idx)`) reemplaza el viejo `monitor_index: Option<usize>` de `ScreenCapturer::start_recording`; `list_windows()`/`WindowInfo` en paralelo a `list_monitors`, usando `windows_capture::window::Window::enumerate()` (confirmado leyendo el source: existe y sigue el mismo patrón 1-based que `Monitor`). Comando Tauri `list_windows` + `start_recording` con nuevo parámetro `window_index` (gana sobre `monitor_index` si está presente). UI: `App.tsx` con selector de "Pantalla completa" vs. "Una ventana".
- **Grabación resistente a crashes** (`crates/capture/src/raw_encoder.rs`, nuevo): la toma cruda dejó de encodearse con el `VideoEncoder` interno de `windows-capture` (solo escribe el `moov`/índice al final, en `finish()` — un crash a mitad de grabación deja un mp4 con frames válidos pero irreproducible) y pasa a un proceso `ffmpeg` propio alimentado con los frames crudos BGRA8 (`frame.buffer()?.as_nopadding_buffer()`, confirmado leyendo `frame.rs` del crate) usando mp4 **fragmentado** (`-movflags frag_keyframe+empty_moov+default_base_moof`, `-g <fps>` para acotar la pérdida a ~1s). Esto cumple CLAUDE.md regla 7 al pie de la letra. Bitrate alto (50M) a propósito, es la toma intermedia, no la entrega final.
- **Cursor reconstruido suavizado**: nuevo campo `Project.cursor_path: Vec<CursorPathPoint>` (downsampled a 30Hz salvo clicks, que siempre se guardan — `commands/recording.rs::build_cursor_path`, con tests). `crates/compositor/src/cursor_path.rs` (nuevo): suavizado por media móvil exponencial (constante de tiempo 90ms) sobre la trayectoria cruda, con tests incluyendo un caso de salto brusco verificando que efectivamente rezaga y converge. El cursor se dibuja en `style_pass.rs` (`apply_style_ex`, nueva función; `apply_style` de 4 args queda como wrapper fino para no romper call sites/tests de Fase 2) como un círculo blanco reconstruido con anillo de acento en click — deliberadamente **no** es un renderer de la flecha nativa de Windows (hubiera necesitado rasterizar un path vectorial real; un punto/dot reconstruido es la simplificación consciente para no irse al extremo). `exporter::render_and_export` arma el marcador transformando la posición del cursor por el `crop_rect` vigente de la cámara (si el mouse quedó fuera del zoom actual, no se dibuja). El preview del editor (`commands/preview.rs`) también lo muestra (sin motion blur, que no aplica a un frame estático).
- **Motion blur real**: en vez de un blur direccional simple, `radial_zoom_blur` en `style_pass.rs` es un blur "zoom" real (arrastra cada pixel hacia el centro en 8 muestras y promedia) aplicado sobre el contenido ya recortado, con la intensidad calculada en `exporter.rs` a partir de la velocidad de cambio de tamaño del `crop_rect` entre frames (una transición default del zoom-engine llega a media intensidad; sin cambio de zoom, intensidad 0 y cero costo extra). **Bug real encontrado y corregido a mano en esta misma sesión** (sin poder compilar para confirmarlo, solo trazando la aritmética): la primera versión centraba el blur con la convención "centro de pixel" (`x+0.5`) en vez de "índice de pixel", lo que producía un corrimiento sistemático de 1px incluso con blur en 0 — corregido a `cx=(w-1)/2.0` sin el offset de 0.5, y el test que lo hubiera detectado (`motion_blur_leaves_the_exact_center_pixel_unchanged`) quedó escrito para la próxima corrida de `cargo test`.
- **Auto-update: deliberadamente NO tocado.** `tauri-plugin-updater` valida su config (`endpoints`/`pubkey`, ambos campos obligatorios) al registrar el plugin en `main.rs` — si el plugin se registra sin una key real generada (`pnpm tauri signer generate`, requiere un build funcional que hoy no se puede correr), la app entera puede fallar a arrancar. Se decidió no dejar ese riesgo sin poder probarlo. Pendiente explícito para cuando el toolchain esté operativo: generar el keypair, agregar `plugins.updater` a `tauri.conf.json` con la pubkey real + el endpoint de GitHub Releases, y registrar el plugin recién ahí.
- **Pendiente real, en orden**: (1) reiniciar Windows, (2) `cargo test --workspace` + `cargo clippy --workspace --all-targets -- -D warnings` y arreglar lo que salte, (3) probar la app de verdad grabando (sigue pendiente desde Fase 2 — selección de ventana, cursor y motion blur nunca se vieron en pantalla todavía), (4) auto-update si se decide que vale la pena para este proyecto (uso personal, sin backend — prioridad baja).

---

## 2026-08-21 — Fase 2 completa: editor post-grabación (timeline, easing, preview, estilo)

### Sesión 1 — Auditoría de estado + brainstorming/spec/plan + implementación completa de Fase 2

- **Pedido**: "cómo vamos con la app, quiero que quede 100% terminada, lista para grabar los mejores videos" → tras la auditoría (Fase 1 código completo pero sin demo real validada por Manuel; Fase 2/3 sin empezar), Manuel pidió avanzar al máximo sin poder validar con una grabación real todavía, dejando la validación visual para después.
- Se decompuso el trabajo restante en 2 sub-proyectos (Fase 2 — editor, Fase 3 — pulido/motion blur/cursor/crash-recovery) siguiendo el flujo de `superpowers:brainstorming` (arquitectura/preguntas/diseño) → spec en `docs/superpowers/specs/2026-08-21-fase2-editor-design.md` → plan en `docs/superpowers/plans/2026-08-21-fase2-editor.md` (12 tareas) → ejecución inline (sin subagentes, mismo contexto ya cargado) task por task con TDD real.
- **`crates/project`**: `Rect::clamp_into_unit_square()` — clampea ediciones manuales de keyframes a quedar dentro de [0,1]×[0,1] (4 tests).
- **`crates/compositor`**: `RawFrameReader::spawn_seeked()` (decodifica desde un timestamp via `-ss` de ffmpeg, para el preview sin tener que leer toda la toma desde el principio); `style_pass` nuevo — segunda etapa de composición **en CPU** (no un segundo shader WGSL: resultado pixel-exacto y testeable sin device de GPU) que aplica background (6 presets de gradiente), padding, esquinas redondeadas (SDF de rectángulo redondeado, técnica estándar de Inigo Quilez) y sombra suave sobre el frame ya compuesto por `Compositor::composite_frame`. 6 tests unitarios pixel-exactos, todos verdes a la primera.
- **`crates/exporter`**: `render_and_export` ahora aplica `style_pass` a cada frame antes de encodear — desde ahora todo export sale con el `Style` del proyecto (default: padding 6%, esquinas 18px, sombra activa), no solo crop+scale plano. Verificado con el test de integración real end-to-end (`#[ignore]`, GPU+ffmpeg reales contra `spikes-output/fase0_capture.mp4`): sigue exportando un mp4 válido, ahora con el estilo aplicado.
- **`apps/desktop/src-tauri`**: comando `render_preview_frame` (decode+cámara+composite+style a baja resolución ~480p, PNG codificado en base64) y comandos `get_project`/`update_keyframe`/`add_keyframe`/`delete_keyframe`/`update_style` (CRUD del `.szproj`, clampean el rect y marcan `KeyframeSource::Manual` en cada edición). Se agregaron `compositor`, `png` y `base64` como dependencias nuevas de `screenzoom_desktop` (antes el crate `compositor` solo se usaba indirectamente vía `exporter`). Detalle no trivial verificado leyendo el source real de `tauri-macros` (mismo hábito que con `wgpu` en sesiones previas): un comando Tauri **sync** (no `async fn`) ya corre en el threadpool bloqueante de Tauri por sí solo — no hacía falta envolver `render_preview_frame` en `spawn_blocking` manual (el primer intento sí lo hacía y no compilaba: `JoinHandle` de Tauri no tiene `.join()` síncrono).
- **UI** (`apps/desktop/src/`): `TimelineOverlay.tsx` (rect de zoom arrastrable/redimensionable con 8 handles sobre el preview, pointer events nativos, sin librerías nuevas), `KeyframeTrack.tsx` (pista horizontal con drag para mover/redimensionar keyframes, doble click para borrar), `StylePanel.tsx` (fondo/padding/esquinas/sombra + selector de easing del keyframe seleccionado — los 3 presets de easing ya estaban soportados por el backend desde Fase 1). `App.tsx` integrado: al detener una grabación se carga el `.szproj`, se renderiza el preview (debounced 100ms) en cada movimiento del scrubber o del timeline, y toda edición se persiste en el acto.
- **Verificación real, no solo "compila"**: `cargo test --workspace` (todas las suites en verde, incluida la integración real de exporter con GPU+ffmpeg), `cargo clippy --workspace --all-targets -- -D warnings` limpio, `pnpm exec tsc -b --noEmit` y `pnpm exec vite build` limpios, `pnpm tauri dev` levantado real (build de 33s, ventana corriendo sin panics ni errores en el log) y cerrado limpio al terminar (sin procesos huérfanos).
- **Pendiente real — bloquea el criterio de "listo" formal de Fase 2** (`docs/PROMPT_AGENTE_DEV.md`): falta la validación visual de Manuel usando la app de verdad — grabar algo, arrastrar el rect de zoom, tocar el panel de estilo, exportar, y confirmar que el resultado final se ve bien. No se pudo hacer en esta sesión porque Manuel no podía grabar en el momento; todo el código quedó listo para esa prueba apenas se pueda.
- **Pendiente explícito, fuera de esta sesión**: sub-proyecto B (Fase 3 — motion blur real, cursor reconstruido/suavizado, selección de ventana como fuente, `tauri-plugin-updater`, resiliencia a crashes de la toma cruda — este último ya identificado como no trivial: `windows-capture` 2.0.1 no expone MP4 fragmentado, va a necesitar una decisión de diseño propia, no un one-liner).

---

## 2026-08-16 — Scaffold completo del proyecto: Fase 0 (spike de riesgo técnico) y Fase 1 completa (pipeline end-to-end grabación→zoom→export)

### Sesion 1 — Fase 0: spike de captura (`windows-capture`) + input tracking (`rdev`)

- **Pedido**: leer `docs/ARQUITECTURA.md`/`docs/PROMPT_AGENTE_DEV.md` completos y ejecutar la Fase 0 sin detenerse por bloqueos, documentando pendientes al final.
- Scaffold del repo desde cero: workspace de Cargo + pnpm, `.gitignore`, `.env.example`, `README.md`, siguiendo la estructura de `ARQUITECTURA.md` §5. `git init` (no existía repo git todavía) + remote `origin` a `github.com/lionegmanuel/foveo-app`.
- `crates/capture`: spike (`src/bin/spike_record.rs`) grabando el monitor primario a mp4 H264 vía `Monitor::primary()` de `windows-capture` (sin picker interactivo, deliberado para la Fase 0).
- `crates/input-tracker`: spike (`src/bin/spike_log_input.rs`) con `rdev::listen` logueando eventos de mouse a JSONL con timestamp de reloj de pared.
- **Primera corrida**: mp4 de 60s válido pero el input log salió vacío — no era un bug, nadie tocó el mouse durante la ventana de grabación. Se agregó auto-verificación con `rdev::simulate` de 4 movimientos sintéticos (deliberadamente **solo** `MouseMove`, nunca clicks reales — inyectar un click real en el escritorio del usuario sin saber qué hay debajo del cursor es un riesgo innecesario).
- **Verificación real**: mp4 validado con `ffmpeg -v error` (decodifica sin errores, 1920x1080@60fps H264, ~110MB). Los 4 eventos sintéticos quedaron logueados con timestamps reales del hook de `rdev`, correlacionados matemáticamente contra `recording_started_wall_ms` de la metadata del video — offsets resultantes (~9.57s/12.57s/15.57s/18.57s) coinciden exacto con el spacing de 3s simulado, confirmando que ambos comparten el mismo reloj.
- Manejo de clicks (no probado en vivo por la razón de arriba) cubierto en cambio con 5 tests unitarios puros sobre la lógica de formateo.
- **Aviso de privacidad**: la toma cruda grabó lo que realmente estaba en pantalla del usuario en ese momento (comportamiento esperado de cualquier screen recorder) — queda 100% local en `spikes-output/` (gitignored, nunca comiteado ni transmitido).
- Limpieza de un directorio `scratch_test/` residual de un smoke-test temprano del toolchain (Rust/MSVC/pnpm).

---

### Sesion 2 — Fase 1 completa: los 8 pasos de `PROMPT_AGENTE_DEV.md` con código real y tests, no solo scaffolding

- **Pedido**: "avanzá todo lo posible" tras cerrar la Fase 0.
- **`apps/desktop`** (paso 1 del scaffold pendiente de Fase 0): Vite + React 19 + TypeScript + Tailwind v4, wireado a Tauri v2 (`tauri.conf.json`, `capabilities/default.json`, iconos generados con `tauri icon` a partir de un placeholder). Verificado con `pnpm tauri dev` real: Vite + `cargo build` + ventana levantan sin errores (log de la corrida confirmado).
- **`crates/project`**: schema completo del `.szproj` (`Project`/`RawTake`/`ZoomKeyframe`/`Rect`/`Easing`/`KeyframeSource`/`Style`/`ExportSettings`) con `serde`. 5 tests (roundtrip JSON, anclaje contra el ejemplo de `ARQUITECTURA.md` §3.5, defaults al omitir campos opcionales, save/load a disco, error de IO en archivo inexistente).
- **`crates/zoom-engine`**: clustering de clicks por cercanía temporal + zoom-out por inactividad configurable (constantes documentadas: `CLICK_CLUSTER_WINDOW_MS`, `INACTIVITY_ZOOM_OUT_MS`, `ZOOM_TARGET_SIZE`, `TRANSITION_DURATION_MS`). 12 tests cubriendo exactamente lo pedido en el paso 4: clicks aislados, clusters (incluyendo clustering encadenado), inactividad (entre clusters y al final de la grabación), el caso borde de cero eventos de input, clamping de `target_rect` en los bordes del frame.
- **`crates/capture`/`crates/input-tracker`** formalizados de spike a trait real: `ScreenCapturer`/`InputSource` + impls `WindowsScreenCapturer`/`RdevInputSource` con `start`/`stop` no bloqueante. Se sumó `list_monitors()` + selector de monitor por índice (antes solo grababa el primario). Tests reales contra hardware: `start_then_stop_produces_a_playable_take` (graba 2s de pantalla de verdad, `#[ignore]` por default) y `list_monitors_returns_at_least_one_real_monitor` (no `#[ignore]`, confirmado con los 2 monitores reales de esta máquina).
- **`crates/compositor`**: pipeline `wgpu` real de crop+scale (shader WGSL de "fullscreen triangle", BGRA8 en toda la pipeline para no convertir entre lo que produce `windows-capture` y lo que consume `ffmpeg`). API de `wgpu` 30 verificada leyendo el source descargado en `~/.cargo/registry/src/` antes de escribir código — encontró y corrigió varios breaking changes reales vs. la memoria de entrenamiento (`InstanceDescriptor::new_without_display_handle()`, `immediate_size` en vez de `push_constant_ranges`, `multiview_mask`, `PollType::wait_indefinitely()`). `camera_path::camera_rect_at` (interpolación pura de la cámara entre keyframes, sin GPU) con 9 tests. `decode::RawFrameReader` para leer la toma cruda vía un proceso `ffmpeg -f rawvideo`. 3 tests de integración real contra el device de GPU de esta máquina (no `#[ignore]`, sin side effects de pantalla) — reproducción exacta de color sólido y crop verificado descartando la mitad del frame.
- **`crates/exporter`**: pipe de frames compuestos a un proceso `ffmpeg` de encoding+mux, usando `libopenh264` (LGPL) en vez de `libx264` (GPL) por el riesgo de licencia documentado en `ARQUITECTURA.md` §3.6. `render_and_export()` ata la pipeline completa: decode → `camera_rect_at` por frame → `Compositor::composite_frame` → encode. Test de integración real (`#[ignore]` por default): recorta 1s del mp4 real de la Fase 0, corre la pipeline completa de punta a punta (GPU + ffmpeg reales) en ~4.4s, valida el mp4 resultante con `ffmpeg -v error`.
- **Comandos Tauri** (`apps/desktop/src-tauri/src/commands/`): `list_monitors`, `start_recording`, `stop_recording` (arma el `.szproj` parseando el JSONL crudo del input-tracker a `zoom_engine::InputEvent` — conversión de timestamps de reloj de pared a relativos y de píxeles a coordenadas normalizadas 0..1, 2 tests unitarios propios), `export_project` (corre `render_and_export` en un thread aparte, progreso/resultado por eventos Tauri `export-progress`/`export-finished`/`export-error`, nunca bloqueando la respuesta del comando).
- **UI mínima** (`apps/desktop/src/App.tsx` + `src/lib/commands.ts`): selector de monitor, botón grabar/detener, botón exportar, progreso vía los eventos de arriba. Sin timeline editable (eso es Fase 2).
- **Verificación real en cada capa**, no solo "compila": `cargo clippy --workspace --all-targets -- -D warnings` limpio, `cargo test --workspace` con 37 tests en verde, `pnpm exec tsc -b --noEmit` + `pnpm exec vite build` limpios, `pnpm tauri dev` levantado sin errores dos veces (la primera corrida dejó un `vite`/`msedgewebview2.exe` huérfano ocupando el puerto 1420 que hubo que matar a mano antes de relanzar).
- **Limitación de verificación honesta**: se intentó tomar una captura de pantalla de la ventana de la app para confirmar visualmente el `list_monitors()` con datos reales, pero Windows no deja que un proceso en background le robe el foco a la ventana activa del usuario (DaVinci Resolve en este caso) — se descartó forzar el foco tras 3 intentos para no seguir capturando contenido no relacionado en pantalla. La verificación quedó apoyada en los logs del dev server (sin errores) + la batería de tests automatizados/hardware real de abajo, no en una captura visual de la UI.
- Commits de la sesión: `bb69c73` (Fase 0), `bc70803`/`3606c11`/`90c1c7b`/`303fc97`/`f0520a8`/`639c66b` (Fase 1, uno por sub-tarea coherente).
- **Pendiente real — nada de esto bloquea seguir**:
  - El criterio de "listo" de Fase 1 (`docs/prompt_agente_dev.md`) formalmente exige grabar una demo real de 2-3 min **con la app** (no un test sintético) y confirmar visualmente que el zoom automático sigue los clicks en el mp4 exportado — no hecho todavía, requiere criterio humano de Manuel sobre calidad visual, no es verificable por un test automatizado.
  - Script de descarga + verificación de hash del sidecar de `ffmpeg` (`ARQUITECTURA.md` §5, `CLAUDE.md` regla 5) no implementado — hoy el proyecto usa cualquier `ffmpeg` de PATH del sistema (o `FFMPEG_BINARY_PATH` si está seteada), no el binario bundleado real.
  - `rdev` 0.5.3 no tiene `unlisten()` real: `InputRecordingHandle::stop()` solo apaga la escritura, el hilo del hook global de OS queda vivo (idle) hasta que el proceso entero termina — documentado en `crates/input-tracker/src/lib.rs`, no es un bug, es una limitación conocida del crate con upgrade path documentado en `ARQUITECTURA.md` §3.6 (hook nativo `WH_MOUSE_LL`).
  - El mp4 de la toma cruda trae un track de audio AAC vacío pese a pedir `disabled(true)` — cosmético, quirk de `windows-capture`, no bloquea nada.
  - Sin picker interactivo de fuente (monitor/ventana) — hoy `start_recording` recibe el índice de monitor directo desde el `<select>` de la UI, no hay diálogo nativo de Windows. Cumple el requisito funcional de "selector de monitor" de la Fase 1, el picker nativo queda como posible mejora de UX, no bloquea nada.
  - Fase 2 (editor post-grabación: timeline, presets de easing, preview, panel de estilo) y Fase 3 (motion blur, cursor reconstruido, selección de ventana, auto-update, manejo robusto de crashes) explícitamente fuera de alcance hasta que se pidan — ver `docs/prompt_agente_dev.md` sección "Qué NO hacer".

---

### Sesion 3 — Cierre del pendiente de sidecar de ffmpeg: descarga + verificación de hash + wiring completo en Tauri

- **Pedido**: "hay algo con lo que YA mismo puedas seguir y avanzar?" — se identificó el pendiente de la Sesión 2 (script de descarga/hash de `ffmpeg` no implementado) como el único ítem 100% técnico, sin necesitar criterio de Manuel.
- **`scripts/setup-ffmpeg.mjs`** (nuevo, `pnpm setup:ffmpeg`): descarga el build **LGPL-only real** de `BtbN/FFmpeg-Builds` (`n8.1`, confirmado con `ffmpeg -version` que trae `--disable-libx264 --disable-libx265` + `--enable-libopenh264` + NVENC/AMF/QSV/Media Foundation), calcula el SHA256 mientras descarga y aborta sin instalar nada si no coincide con el valor pineado. Extrae `ffmpeg.exe` del zip (via el `bsdtar` nativo de `System32`, no el `tar` de Git Bash — el GNU tar interpreta rutas tipo `C:\...` como specs remotos SSH y falla) y lo deja en `sidecars/ffmpeg/ffmpeg-<target-triple>.exe`.
- **URL y hash reales, no placeholders**: se descargó el zip de verdad (~146MB), se calculó su SHA256 real (`fc9d3072...b9c6c`) y se lo pineó tanto en el script como en `.env.example` (`FFMPEG_DOWNLOAD_URL`/`FFMPEG_EXPECTED_SHA256`, antes vacíos).
- **`tauri.conf.json`**: `bundle.externalBin` agregado. Primer intento con la ruta relativa mal calculada (`../../sidecars/...`, faltaba un nivel — `src-tauri/` está 3 carpetas abajo de la raíz del repo, no 2) tiró `resource path ... doesn't exist` en `cargo build`; corregido a `../../../sidecars/ffmpeg/ffmpeg`.
- **Verificado leyendo el source de `tauri-build`** (mismo hábito que con `wgpu` en la Sesión 2): `copy_binaries()` pela el sufijo de target-triple del nombre SIEMPRE al copiar al `target_dir` — tanto en `cargo build`/dev como en el build final — así que el sidecar termina siendo literalmente `ffmpeg.exe` al lado del ejecutable principal en los dos casos, no hace falta lógica distinta por ambiente.
- **`main.rs`**: `resolve_and_set_ffmpeg_env_var()`, llamada primero en `main()` antes de `tauri::Builder`, setea `FFMPEG_BINARY_PATH` (que ya leían `crates/compositor`/`crates/exporter` desde la Fase 1) al sidecar real si el usuario no la seteó a mano en `.env`. `std::env::set_var` es `unsafe` en esta edición (2024); seguro acá por ser el único punto de escritura, antes de que se levante ningún thread.
- **Verificación real, no solo "compila"**: script corrido de punta a punta (descarga real, hash verificado OK, extracción, `ffmpeg-x86_64-pc-windows-msvc.exe` de 108.7MB funcional — confirmado con `-version`), corrida repetida confirmando idempotencia (detecta que ya existe, no vuelve a descargar), `cargo build` en `apps/desktop/src-tauri` confirmado copiando el sidecar a `target/debug/ffmpeg.exe`, `cargo clippy -p screenzoom_desktop --all-targets -- -D warnings` limpio.
- **Deliberadamente no se tocó `UPDATES.md` en el commit de este trabajo** (`402743c`) — se hizo en un commit aparte, recién ahora, porque el pedido explícito de actualizarlo llegó en un mensaje posterior; ver CLAUDE.md sección 6 ("escritura solo cuando el usuario lo pide directamente").
- **Pendiente real**: ninguno técnico nuevo. El único pendiente que sigue abierto de toda la Fase 1 es el mismo de la Sesión 2 — la demo real de 2-3 min grabada con la app y la confirmación visual de Manuel sobre la calidad del zoom en el mp4 exportado.
